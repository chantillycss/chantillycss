---
layout: layouts/base.njk
---
<article class="container center stack post">
  <h1>{{ title }}</h1>
  <p><small>{{ page.date.toLocaleDateString('fr-FR', { year: 'numeric', month: 'long', day: 'numeric' }) }}
     — lecture {{ lecture }} min</small></p>
  {{ content | safe }}
</article>
